name: Build and publish Docker image

on:
  push:
    branches: [ "main" ]
    tags:
      - 'v*'
  workflow_dispatch:

permissions:
  contents: read
  packages: write

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up QEMU (for multi-platform builds)
        uses: docker/setup-qemu-action@v2

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v2
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Generate metadata for main branch (latest)
        id: meta_branch
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        uses: docker/metadata-action@v4
        with:
          images: ghcr.io/${{ github.repository_owner }}/norcom_radio
          tags: |
            type=ref,ref=refs/heads/main,name=latest
            type=sha,format=short

      - name: Generate metadata for tag
        id: meta_tag
        if: startsWith(github.ref, 'refs/tags/')
        uses: docker/metadata-action@v4
        with:
          images: ghcr.io/${{ github.repository_owner }}/norcom_radio
          tags: |
            type=ref,event=tag
            type=sha,format=short

      - name: Build and push Docker image
        uses: docker/build-push-action@v4
        with:
          context: .
          file: ./Dockerfile
          push: true
          tags: ${{ steps.meta_branch.outputs.tags || steps.meta_tag.outputs.tags }}
          labels: ${{ steps.meta_branch.outputs.labels || steps.meta_tag.outputs.labels }}
